// array slice, splice, join

// array slice
let num_list = [0, 1, 2, 3, 4, 5, 6, 7];

console.log(num_list);

console.log(num_list.slice(0, 2));

// console.log(num_list.splice(0, 4));




console.log(num_list.join(" is a number"+" "+" ,"));

var sing = {};

if (sing) {
    console.log('not');
} else {
    console.log('yes');
}
